{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build Only",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "cmd.exe",
            "args": [
                "/c",
                "echo Build completed successfully! && echo DLL and EXE copied to root directory && pause"
            ],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "externalTerminal",
            "preLaunchTask": "Copy DLL to Root"
        }
    ]
}